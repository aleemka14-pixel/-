# Frontend Pages

This directory maps conceptually to `/src/pages` for routing and page-level layout components.

In this single-page architecture, views are rendered dynamically based on active state overlays and routes inside `/src/App.tsx`.
