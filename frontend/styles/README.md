# Frontend Styles

This directory maps conceptually to `/src/styles`. It contains the Tailwind CSS integration and global typography/theming definitions.

To adjust styling, edit `/src/styles/index.css`.
