# Frontend Components

This directory maps conceptually to `/src/components`. It contains modular, reusable UI components for the deposit, withdrawal, wallet, and admin views.

To edit or add UI components, modify files in the `/src/components` folder directly.
