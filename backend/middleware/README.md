# Express & Serverless Middlewares

This directory contains reusable middleware functions to intercept, authorize, and validate requests.

## Key Responsibilities
- User authentication and session verification (Firebase Auth verification).
- Request rate-limiting to prevent brute force and denial-of-wallet attacks.
- Input validation schemas (Joi, Zod) before handling operations.
- Global error logging and handling.
