# Application Configuration

This directory contains configuration files for third-party libraries, deployment tooling, and global environment state.

## Key Responsibilities
- Managing payment gateway keys and URLs.
- Setting up network presets (ERC-20, TRC-20, native coins).
- Configuring CORS lists and API version controls.
